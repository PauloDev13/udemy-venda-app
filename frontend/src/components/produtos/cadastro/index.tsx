import { NextPage } from 'next';

import { Layout } from '~/components/layout';

export const CadastroProduto: NextPage = () => {
  return (
    <Layout title="Cadastro de Produtos">Aqui vai ficar o formulário</Layout>
  );
};
